# Divina Essência — Patch v8 (incremental, seguro)

Siga os passos descritos na conversa: copie os arquivos, rode o SQL desejado e adicione a linha abaixo no topo de src/styles/index.css:

```css
@import './tokens.css';
```
